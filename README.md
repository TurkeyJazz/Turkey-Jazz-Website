# Scores Page
this branch is dedicated to work on the score browser and score viewer, commit other changes to other branches. 
example scores are already in the data folder under scores, use those for testing purposes, don't add more to this branch. if more scores are needed for testing(searching and indexing), use a local folder or pull from the existing cloud folder. 
only use the newest version of notation files: musescore 4.0 or later, musicxml 4.0
